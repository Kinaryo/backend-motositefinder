<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Motors</title>
</head>
<body>
    <h1>Update</h1>
    <form action="/pages/<%= motor._id %>?_method=PUT" method="POST">
        <div class="form-group">
            <label for="title">Title</label>
            <input type="text" name="motor[title]" id="title" value="<%= motor.title %>">
        </div>

        <div class="form-group">
            <label for="licensePlate">License Plate</label>
            <input type="text" name="motor[licensePlate]" value="<%= motor.licensePlate %>"> 
        </div>

        <div class="form-group">
            <label for="model">Model</label>
            <input type="text" name="motor[model]" value="<%= motor.model %>">
        </div>

        <div class="form-group">
            <label for="description">Description</label>
            <input type="text" name="motor[description]" value="<%= motor.description %>">
        </div>

        <div class="form-group">
            <label for="dateTime">Date - Time</label>
            <input type="datetime-local" name="motor[dateTime]" id="dateTime" value="<%= motor.dateTime %>" >
        </div>

        <div class="form-group">
            <label for="image">Image URL</label>
            <input type="text" name="motor[image]" value="<%= motor.image %>">
        </div>

        <div class="form-group">
            <button type="submit">Submit</button>
        </div>
    </form>
</body>
</html>
